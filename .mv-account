# .mv-account - open (portable) account descriptor
name = mv_package
version = 1
openaccount = 1
hash = lmdb
permit prog:MVPKG = mkdir rm tar uname od sed grep sort wc tr ldconfig mkpkg udt-callc-build git sudo chown ls id
